# NutriTrack AI Tab-wise Specs

This folder breaks down `docs/NutriTrack_AI_Implementation.md` into tab-specific implementation docs.

## Tabs

- `dashboard.md`
- `add-meal.md`
- `chat.md`
- `profile.md`

## Notes

- These files are derived from the master implementation spec.
- Shared platform requirements (Supabase, Edge Functions, PWA, security, offline) are repeated in each tab where relevant so each tab can be implemented independently.
- Use the master file as the final source of truth if any wording conflict appears.
